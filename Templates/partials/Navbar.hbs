
<!-- Minimal Navbar with only logo, shop name and cart button -->
<nav class="navbar fixed-top bg-white shadow-sm">
  <div class="container">
    <!-- Brand section with logo and name -->
    <a class="navbar-brand d-flex align-items-center" href="/{{link}}">
      <img src="{{logoLink}}" alt="{{shopname}} logo" class="me-2" width="40" height="40">
      <span class="fw-bold">{{shopname}}</span>
    </a>
    
    <!-- Cart button with counter - right aligned -->
    <a href="/{{link}}/Cart" class="btn btn-primary position-relative px-3 rounded-pill">
      <i class="fa-solid fa-cart-shopping me-2"></i>
      <span>Cart</span>
      <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger cart-count">
        0
        <span class="visually-hidden">items in cart</span>
      </span>
    </a>
  </div>
</nav>

<!-- Script for updating cart count -->
<script>
  document.addEventListener("DOMContentLoaded", function() {
    // Cart counter update
    const updateCartCount = () => {
      // This would typically fetch from your cart service/local storage
      const count = localStorage.getItem('cartCount') || 0;
      const badges = document.querySelectorAll('.cart-count');
      badges.forEach(badge => {
        badge.textContent = count;
        badge.style.display = count > 0 ? 'block' : 'none';
      });
    };
    
    // Initialize cart count
    updateCartCount();
    
    // Custom event listener for cart updates
    window.addEventListener('cartUpdated', updateCartCount);
    
    // Handle image loading if needed
    if (document.querySelector('img[data-src]')) {
      const images = document.querySelectorAll('img[data-src]');
      images.forEach(img => {
        img.src = img.getAttribute('data-src');
        img.onload = function() {
          img.removeAttribute('data-src');
        };
      });
    }
  });
</script>

<!-- Add this CSS to your stylesheet -->
<style>
  .navbar {
    padding: 0.75rem 0;
    transition: all 0.3s ease;
  }
  
  .navbar-brand img {
    transition: all 0.3s ease;
    object-fit: contain;
  }
  
  /* Optional: Shrink navbar slightly on scroll */
  .navbar.scrolled {
    padding: 0.5rem 0;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
  }
  
  .navbar.scrolled .navbar-brand img {
    width: 32px;
    height: 32px;
  }
  
  /* Add some minimal responsive handling */
  @media (max-width: 576px) {
    .navbar-brand span {
      font-size: 0.9rem;
    }
    
    .btn {
      padding-left: 0.75rem !important;
      padding-right: 0.75rem !important;
    }
  }
</style>
<script>


  document.addEventListener("DOMContentLoaded", function () {
    const images = document.querySelectorAll('img[data-src]');
    images.forEach(img => {
      img.src = img.getAttribute('data-src');
      img.onload = function () {
        img.removeAttribute('data-src');
      };
    });
  });
</script>